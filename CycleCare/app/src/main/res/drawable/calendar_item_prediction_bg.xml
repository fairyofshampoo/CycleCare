<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/cute_yellow" /> <!-- Fondo blanco -->
    <stroke
        android:width="2dp"
        android:color="@color/light_gray" /> <!-- Borde gris claro -->
    <corners android:radius="8dp" />
</shape>